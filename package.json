{
  "name": "benchmark-suite",
  "version": "1.0.0",
  "main": "index.js",
  "type": "module",
  "private": true,
  "scripts": {
    "build:architectures": "rm -rf ./packages/core/src/architectures/* && python ./scripts/collect_models.py && npm run format",
    "format": "npx prettier --write .",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "devDependencies": {
    "prettier": "3.4.2"
  },
  "author": "Hugging Face",
  "license": "Apache-2.0"
}
